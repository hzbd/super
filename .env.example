# Copy to `.env` (gitignored). Required for make build / check / docker —
# common/keys/ is empty in git; keys are fetched from Manager every build.
# Create an API token with `products.read` (Manager Settings → API tokens).

MANAGER_BASE=http://127.0.0.1:8787
MANAGER_TOKEN=
